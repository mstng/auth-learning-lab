# v1.6 保存場所とDBの対応表の検証

検証日: 2026-09-21

- Next.js本番ビルドとTypeScript型検査に成功。
- 実際のPGliteと本番サーバーを使うブラウザテストのうち、変更に関係する3シナリオを再実行し、すべて成功（25.4秒）。基本ガイド完走、失敗ガイド完走、Cookie消失を期限切れと誤判定しないケースを含む。
- ブラウザ「保存」・サーバー「一時利用」・DB「記録」の表示を確認。番号生成後はサーバーに実際の番号が表示され、結果画面ではサーバーの番号表示がなくなる。
- DBの対応表に表示する番号・利用者ID・有効期限が実データと一致することを確認。利用者は対象のusersレコードと対応し、有効期限は日本時間で表示する。
- DBだけを期限切れにしてもCookieと対応表の番号は残る。期限切れ状態、再ログイン後の新しい番号と期限内表示、ログアウト後の対応表の消失を確認。
- PC 1440px・スマートフォン390pxの画面を目視確認。DBの項目はPCでは横並び、狭い画面では同じカード内で縦並びになる。横はみ出しなし。PCの基本ガイドでは結果と次の操作が画面内に収まる。
- 基本・失敗ガイドの未処理JavaScriptエラーなし。

サーバーの番号を片付けるのは学習用の図の表示であり、実メモリーの消去を検証するものではない。DBはサーバー側の保存場所であることを図に明記した。

スクリーンショット：screenshots/guided-preview.png、screenshots/guided-mobile.png、screenshots/failure-preview.png、screenshots/failure-mobile.png。

認証・DBの処理は変更なし。以下は過去の検証記録であり、今回すべてを再実行したものではない。

---

# v1.5 予想・失敗ガイド・番号の追跡の検証

検証日: 2026-09-21

- Next.js本番ビルドとTypeScript型検査に成功。
- 実際のPGliteと本番サーバーを使用し、ブラウザ10シナリオを検証。学習画面に関する5シナリオは表示の最終調整後に再実行。
- 予想を選ぶまで実行できず、「まだわからない」でも実行できる。予想と一致・不一致・未予想の各フィードバックを確認。実行後は予想を変更できない。
- 新しい番号を生成する前には、その番号が図のどこにも表示されない。生成後はサーバー、保存後はDB、Cookie保存の説明段階からブラウザに同じ番号が表示される。
- 移動する番号が実際の発行番号と一致すること、DBに渡す場面の矢印、戻る操作との連動を確認。動きを減らす設定では移動アニメーションが無効になる。
- 基本ガイドの4つの問いを完走し、予想が違っていても実験を確認できれば進めることを確認。ログインでProfileを勝手に取得しない。
- 失敗ガイドの5つの問いを完走。誤ったパスワードでは番号を作らず、DBだけの期限切れでは同じCookieを維持し、期限切れの番号を送ると401になり、再ログインで新しい番号へ入れ替わる。
- 同じ番号のまま「Cookieあり・DB期限切れ・利用拒否」が図に表示される。再ログインの番号生成時点ではブラウザに古い番号、サーバーに新しい番号が表示される。
- Cookieを意図的に消して返る401を、期限切れの番号による401と誤判定しない。進捗や予想へのフィードバックは付けず、再開始を案内する。
- 操作後の観測に失敗した場合は判定を保留し、再試行できる。基本ガイド途中の失効時も再開始できる。
- 既存の自由実験、HttpOnly Cookie、自然失効、Passwordのみの照合、権限不足、入力エラー、ロードマップの検証も成功。
- PC 1440px・スマートフォン390pxで画面を目視確認。ページ全体と比較表の横はみ出しなし。基本・失敗ガイドの未処理JavaScriptエラーなし。

初回の1シナリオは、テスト側が予想欄の表示前に次の操作を試みて待ち時間を超過した。予想欄の表示を待って選択するようテストを修正し、再検証した。

スクリーンショット：screenshots/prediction-preview.png（実行前の予想）、screenshots/guided-preview.png（基本ガイドの結果）、screenshots/failure-preview.png（番号が残る期限切れ）、screenshots/failure-mobile.png（スマートフォン）。

認証・DBのサーバー処理は変更していない。以下の過去のドメイン／HTTP統合／再起動検証は今回の再実行に含まない。

---

# v1.4 1つの問いに集中する学習画面の検証

検証日: 2026-09-20

- Next.js本番ビルドとTypeScript型検査に成功。
- 実際のPGliteを使う本番サーバーでPlaywrightブラウザテスト8件が成功（45.4秒）。
- Sessionの初期画面は「順番に学ぶ」。自由実験の入力フォーム・状態欄・インスペクターは同時に表示されない。
- 学習画面の主要ボタンが1つであること、問いが変わると見出しにフォーカスすることを確認。
- ログイン後にProfileを自動取得せず、説明を読み終えて「次の問いへ」を押すまで同じ問いに留まることを確認。
- 図の強調箇所と通信矢印が実行ステップに対応し、「ひとつ戻る」で読み返せることを確認。
- ログイン時の0→1件・実際のCookie番号、ログアウト時の1→0件・実際のCookie削除を確認。
- ProfileのリクエストがパスワードなしのGETであること、ログアウト後の401を別操作として確認。
- 詳細記録は最初は閉じ、開くと完全な実際のIDを確認できる。
- 学習完了後に自由実験へ切り替えても記録と達成が残り、過去のログインを見返しても現在は未ログインのままである。
- 自由実験から学習へ戻り、明示的に開始するとガイドを初期化できる。
- 操作後の確認通信を失敗させた場合は先へ進めず、再試行で確認できる。学習途中のSession失効も、成功扱いせず再開始できる。
- 従来の詳細画面・HttpOnly Cookie・自然失効・Password照合・403・DB期限切れ・400の記録・ロードマップのブラウザ検証も成功。
- 学習フローの未処理JavaScriptエラーは0件。
- PC（1440×1000）ではログイン結果の図・比較・次の操作が画面内に収まる。スマートフォン（390px）でもページ全体の横はみ出しなし。スクリーンショットを目視確認。

スマートフォンの比較表の幅を追加調整し、学習フローのテストを再実行。表自体の横スクロールも発生しないことを検証。

スクリーンショット：screenshots/guided-preview.png（ログインの問いと結果）、screenshots/guided-mobile.png（同じ学習カードのスマートフォン表示）。

今回変更したのは学習UIと実行結果の受け渡し。認証・DBのサーバー処理は変更なし。以下の過去のドメイン／HTTP統合／再起動検証は今回の再実行には含まない。

---

# v1.3 ガイドと実行記録連動の検証

検証日: 2026-09-20

- Next.js本番ビルド・TypeScript型検査に成功。
- 実際のPGliteを使う本番サーバー上でPlaywrightのブラウザテスト7件が成功（40.9秒）。
- ログインが9ステップで終了し、Profileリクエストが自動実行されないことをブラウザのネットワークイベントで確認。
- 別操作のProfileがパスワードなしのGETで実行され、合計13ステップとなることを確認。
- ガイドが同一番号の発行・利用・削除・削除後401を順番に確認して4/4になることを検証。
- スライダーと図の矢印・現在位置・DB追加表示の連動、CookieとDBの変更前後、過去操作への切り替えを確認。
- 過去のログイン記録を選んでも現在の未ログイン状態が変わらないことを確認。
- 未ログイン時の401だけでは達成が付かず、パスワード誤り・権限不足・DB期限切れをそれぞれの結果で判定することを確認。
- 操作後の確認通信を意図的に失敗させ、Cookieが実際に保存されていても、未確認の比較・達成判定は保留されることを確認。
- 入力の長さ制限による400を、過去の正常操作と混ぜずに見返せることを確認。
- 従来のHttpOnly Cookie・自然失効・Password照合・データビューア・ロードマップ・画面遷移のブラウザ検証も成功。
- 1600pxと390pxのスクリーンショットで表示を確認。横はみ出しなし、Sessionおよびガイドフローの未処理JavaScriptエラーなし。

今回の検証はUIと学習用の判定が中心。認証・DBのサーバー処理は変更していない。以下のv1.1のドメイン/HTTP統合/再起動検証は過去の記録であり、v1.3で再実行したものではない。

v1.3のガイド画面はv1.4で更新。同名のスクリーンショットは現在の学習画面を示します。

---

# v1.2 UI改善の検証

検証日: 2026-09-19

- Next.js本番ビルドとTypeScript型検査に成功。
- 実際の組み込みPGliteを使う本番サーバーで、Playwrightの既存3シナリオを更新してすべて成功（約39秒）。
- 図の4場面切り替えが説明だけを変え、ログイン状態を変更しないことを確認。
- 「やさしく／詳しく」で説明と実データの初期表示が変わることを確認。
- ログインの13ステップ、HttpOnly Cookie、DB変更表示、ログ・順番の図との連動、403、DB期限切れ401、ログアウトを確認。
- 15秒の自然失効、パスワード照合だけではSessionを作らないこと、ロードマップ保存、画面遷移を確認。
- PC 1600pxとスマートフォン390pxのスクリーンショットを確認。ページ全体の横はみ出しなし、Sessionフローの未処理JavaScriptエラーなし。
- 今回はUIと説明文の改修。以下のv1.1でのドメイン・HTTP統合・再起動の検証は過去の結果で、今回の再実行には含めない。

---

# PHASE 1〜6 検証結果 — v1.1 Docker不要版

検証日: 2026-09-18

## 結果

| 項目 | 結果 |
|---|---|
| Next.js production build / TypeScript strict | 成功 |
| npm start / Standalone server起動 | 成功 |
| ドメインテスト3件 | 成功 |
| HTTP統合テスト2件 | 成功 |
| Playwrightブラウザーテスト3件 | 成功 |
| PC 1600px / Mobile 390px | 表示確認済み、意図しないページ全体の横スクロールなし |
| ブラウザーの未処理JavaScriptエラー | Sessionフローのテストで0件 |
| アプリの再起動後にusers・Hash・Sessionを保持 | 成功 |
| npm run devで同じ保存データを利用 | 成功 |
| Docker・外部DB | 不要 |

## 検証した動作

- bcrypt照合、異なるSalt、Password長上限、期限判定の境界値。
- Password照合のみではSessionを発行しない。
- 未認証Profileは401、不正Passwordも401、新しいSessionは作らない。
- Session ID生成、DB保存、HttpOnly/SameSite属性、ブラウザーCookieの実保存。
- document.cookieから認証Cookieを読めないこと。
- Login→Profileの9＋4＝13イベントと実際のCookie送信。
- 再ログイン時のID更新・古いIDの拒否。
- 別の学習空間からのSession利用拒否。
- userのAdmin APIは403、adminは200。
- DB期限変更後の401、15秒経過による自然なCookie失効と未認証化。
- Logout時にCookieとDBレコードの両方を削除、削除後のID再利用拒否。
- リロード後のSession継続。
- 同一Origin制約、Content-Type、入力不正、メソッド不正、存在しないAPI。
- レスポンス・イベントにPassword平文が含まれない。
- DBの履歴スナップショット、追加レコードのハイライト。
- Logs / Raw / Sequenceの選択とステップ表示の連動。
- ロードマップの学習済みチェックの端末内保存、画面遷移。

## 実行環境と限界

標準納品構成はNext.js 16.3.5 / React / TypeScript strict / 組み込みPGlite。Node.js 24で検証。
PGliteを同じNode.jsプロセス内で起動し、ファイルシステムへ永続化。検証でも納品版と同じ構成を使用しており、DB・APIのモックではない。Docker、独立したDBプロセス、DB用TCPサーバーは使用していない。
ブラウザーはChromium + Playwright。環境の都合で検証用Chromiumバイナリを使用。学習アプリのOrigin/Cookie検証を無効化する変更は行っていない。

`npm run build`後に`npm run verify:portable`で再現可能。標準の起動手順は`npm ci`と`npm run dev`。`.env`なしで動作を確認した。再起動テストは本番用プロセスを停止して再起動し、同じCookieで同じSessionを照合。その後、開発用プロセスでも同じ保存データを照合した。

## 画面

- screenshots/session-preview.png: Session学習画面の最初の表示範囲
- screenshots/session-desktop.png: PC全体
- screenshots/mobile.png: スマートフォン全体
- screenshots/dashboard.png: ロードマップ

## PHASE対応

1. Project Setup: Next.js/React/TypeScript strict、Node.js起動スクリプト、PGlite内蔵、SQLスキーマ。
2. Dashboard/Learning UI: 18レベルのロードマップ、1〜6の学習リンクと完了チェック。
3. Password Authentication: bcrypt、Salt/Cost解説、成功/失敗、Session非発行の照合API。
4. Session Authentication: ID発行、Cookie、保護API、ローテーション、失効、Logout、基本認可。
5. 今どこ: 6つの責務、現在位置、From/To/Data/Protocol、ステップ・Sequence・ログ連動。
6. Inspectors: HTTP/DB/Browser/Rawの実値、履歴と現在状態の区別、DB変更ハイライト。

JWT以降は本版の対象外。拡張設計のみDESIGN.mdとADRに記載。
